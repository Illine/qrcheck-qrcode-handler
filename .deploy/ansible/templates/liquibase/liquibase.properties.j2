url={{ liquibase.url }}
username={{ liquibase.username }}
password={{ liquibase.password }}
schema=qrcodes
context={{ liquibase.context }}
changeLogFile= {{ liquibase.change_log_file | default('/opt/buildagent/work/qrcheck/qrcode-handler/liquibase/changelog.yaml') }}